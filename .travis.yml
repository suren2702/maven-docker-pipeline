language: java
sudo: required
dist: trusty

cache:
  directories:
  - $HOME/.m2

services:
- docker

before_script:
- docker --version

script:
- make docker-build
- make test

after_script:
- docker images

notifications:
  email:
    on_success: change
    on_failure: always